// Intentionally left blank.
// MainMenuButtonAction is defined in MainMenuController.cs so scene-authored UI can compile
// even if the generated project file still references this path before Unity refreshes it.
